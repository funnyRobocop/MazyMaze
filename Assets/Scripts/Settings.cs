using UnityEngine;
using System.Collections;

public static class Settings
{

    public static bool soundOn;
    public static int control;

    public enum ControlsType { Touch, Swipe, Accelerometer };
}
